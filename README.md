# Geometry Figure Generator

## Run locally
This app uses JavaScript modules, so open it through a local server.

From this folder:

```bash
python3 -m http.server 8000
```

Then open:

- http://localhost:8000

Do not open `index.html` directly with `file://`.
